//Exportar clase base: información de cada mlp//

//No es necesario que este en orden como el resource
export default class Mlp {
    constructor(id,name,kind,image){
        this.id = id;
        this.name = name;
        this.kind = kind;
        this.image = image;
    }
}